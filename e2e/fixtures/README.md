# Playwright E2E fixtures

Mounted into the Playwright container as `/fixtures`.

`sample.pdf` is a tiny placeholder; SMTP attachment coverage uses the backend
`include_sample_attachment` sample (`e2e-sample.pdf`) generated at send time.
