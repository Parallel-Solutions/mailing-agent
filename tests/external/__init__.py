# External integration tests — real providers, real webhooks, real mailbox.
# These tests are disabled unless EXT_STATS_ENABLED=1 is set.
# See EXTERNAL_STATISTICS_TEST_PLAN.md for full specification.
